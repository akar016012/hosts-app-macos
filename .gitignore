# macOS
.DS_Store
*.swp
*~

# Xcode
DerivedData/
xcuserdata/
*.xcuserstate
build/
*.dSYM/

# Swift Package Manager
.build/

# Local code-signing identities (personal — never commit).
.signid
.devid

# Release artifacts produced by release.sh.
*.dmg
*.zip
# Generated Sparkle appcast feed (uploaded as a GitHub Release asset).
appcast.xml
# Sparkle tools downloaded + checksum-verified by release.sh fallback.
.tools/

# Node (legacy, kept from the previous web prototype)
node_modules/

# App-generated /etc/hosts backups
backups/

# Local scratch notes
improvements.md

# Editor / tooling local config (never publish)
.claude/
.vscode/
.idea/
docs/FEATURES.md
temp_docs/*
